<section id="contacto" class="max-w-7xl mx-auto px-4 py-20 bg-slate-100">
  <div class="max-w-3xl mx-auto px-6">
    <h2 class="text-3xl font-bold text-center mb-8">
      Contáctanos
    </h2>

    <form class="bg-white p-8 rounded-xl shadow grid gap-4">
      <input class="border p-3 rounded" placeholder="Nombre" />
      <input class="border p-3 rounded" placeholder="Email" />
      <input class="border p-3 rounded" placeholder="Teléfono" />
      <textarea class="border p-3 rounded" placeholder="Mensaje"></textarea>

      <button
        class="bg-amber-500 text-black py-3 rounded-lg font-semibold hover:bg-amber-400 transition"
      >
        Enviar Mensaje
      </button>
    </form>
  </div>
</section>