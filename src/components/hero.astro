<section
  class="relative w-full min-h-screen overflow-hidden flex items-center justify-center"
  aria-label="Hero principal"
>
  <!-- Fondo fijo SOLO dentro del HERO (recortado por overflow-hidden) -->
  <div
    class="absolute inset-0 bg-[url('/img/01SALA-MONITOREO-PROTEC.jpg')]
           bg-cover bg-center bg-no-repeat bg-fixed"
    aria-hidden="true"
  ></div>

  <!-- Overlay (efecto “transparente”/oscurecido) -->
  <div class="absolute inset-0 bg-protec-950/70" aria-hidden="true"></div>
  <div
    class="absolute inset-0 bg-gradient-to-b from-protec-900/30 via-protec-900/10 to-protec-950/80"
    aria-hidden="true"
  ></div>

  <!-- Contenido -->
  <div class="relative z-10 max-w-5xl mx-auto px-6 text-center text-white -translate-y-12 md:-translate-y-16">
    <!-- tu contenido -->
    <h1 class="text-3xl md:text-5xl lg:text-6xl font-serif tracking-wide uppercase">
      Su seguridad es nuestra<br />responsabilidad
    </h1>

    <p class="mt-6 text-sm md:text-lg text-protec-100">
      PROTEC CHILE Seguridad &amp; Servicios Desde 1988
    </p>

    <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
      <a
        href="/contacto/contactenos"
        class="px-8 py-3 rounded-lg bg-protec-700 text-white text-sm font-semibold tracking-wide
               hover:bg-protec-600 active:bg-protec-800 transition-colors ring-1 ring-white/10 shadow-lg"
      >
        CONTÁCTENOS
      </a>

      <a
        href="/cotizacion"
        class="px-8 py-3 rounded-lg bg-protec-100/90 backdrop-blur
               text-protec-900 text-sm font-semibold tracking-wide
               hover:bg-protec-100 transition-colors ring-1 ring-protec-200 shadow-lg"
      >
        COTICE SU ALARMA
      </a>
    </div>
  </div>

  <!-- Línea inferior -->
  <div class="absolute bottom-0 left-0 w-full h-1 bg-protec-700"></div>
</section>
