---
import { withBase } from "@/utils/url";
---

<section class="w-full bg-protec-900 text-protec-50 py-20">
  <div class="max-w-7xl mx-auto px-6">
    <h2 class="text-4xl md:text-5xl font-bold text-center text-white">
      ¿Por qué elegirnos?
    </h2>

    <p class="mt-4 text-center text-protec-100 max-w-2xl mx-auto">
      Seguridad y experiencia respaldadas por décadas de trabajo y estándares de calidad.
    </p>

    <div class="mt-12 grid gap-8 md:grid-cols-4">
      {[
        { title: "Experiencia desde 1988", desc: "Trayectoria comprobada en seguridad.", badge: "1988" },
        { title: "Certificación ISO 9001", desc: "Procesos auditados y calidad constante.", badge: "ISO" },
        { title: "Confidencialidad y Privacidad", desc: "Protegemos tu información y operación.", badge: "OK" },
        { title: "Monitoreo 24/7", desc: "Vigilancia continua, respuesta inmediata.", badge: "24/7" },
      ].map((item, i) => (
        <div
          class:list={[
            "relative rounded-2xl p-8",
            "bg-protec-800/60 ring-1 ring-protec-600/30",
            "shadow-[0_20px_60px_-30px_rgba(0,0,0,0.8)]",
            "transition hover:bg-protec-800/80",
            i === 1 ? "bg-protec-700/80 ring-protec-500/50" : ""
          ]}
        >
          <div class="flex items-center justify-between">
            <h3 class="text-lg font-semibold text-white">
              {item.title}
            </h3>

            <span class="text-xs px-3 py-1 rounded-full bg-protec-950/40 ring-1 ring-protec-600/30 text-protec-100">
              {item.badge}
            </span>
          </div>

          <p class="mt-4 text-sm text-protec-100 leading-relaxed">
            {item.desc}
          </p>

          <div class="mt-6 h-px w-full bg-protec-600/30"></div>

          <a
            href={withBase("/contacto/")}
            class="
              mt-auto
              inline-flex w-full items-center justify-center
              rounded-xl py-3 text-sm font-semibold
              bg-protec-600 hover:bg-protec-500 active:bg-protec-700
              text-white transition-colors
            "
          >
            Solicitar información
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
