<section class="w-full bg-[#0b0f1a] text-white py-20">
  <div class="max-w-7xl mx-auto px-6">
    <h2 class="text-4xl md:text-5xl font-bold text-center">
      ¿Por qué elegirnos?
    </h2>
    <p class="mt-4 text-center text-white/60 max-w-2xl mx-auto">
      Seguridad y experiencia respaldadas por décadas de trabajo y estándares de calidad.
    </p>

    <div class="mt-12 grid gap-8 md:grid-cols-4">
      {[
        { title: "Experiencia desde 1988", desc: "Trayectoria comprobada en seguridad.", badge: "1988" },
        { title: "Certificación ISO 9001", desc: "Procesos auditados y calidad constante.", badge: "ISO" },
        { title: "Confidencialidad y Privacidad", desc: "Protegemos tu información y operación.", badge: "OK" },
        { title: "Monitoreo 24/7", desc: "Vigilancia continua, respuesta inmediata.", badge: "24/7" },
      ].map((item, i) => (
        <div
          class:list={[
            "relative rounded-2xl p-8",
            "bg-white/[0.06] ring-1 ring-white/10",
            "shadow-[0_20px_60px_-30px_rgba(0,0,0,0.8)]",
            "transition hover:bg-white/[0.08]",
            i === 1 ? "md:-translate-y-2 bg-white/[0.08] ring-white/20" : ""
          ]}
        >
          <div class="flex items-center justify-between">
            <h3 class="text-lg font-semibold">{item.title}</h3>
            <span class="text-xs px-3 py-1 rounded-full bg-white/10 ring-1 ring-white/10 text-white/80">
              {item.badge}
            </span>
          </div>

          <p class="mt-4 text-sm text-white/60 leading-relaxed">
            {item.desc}
          </p>

          <div class="mt-6 h-px w-full bg-white/10"></div>

          <a
            href="/contacto/contactenos"
            class="mt-6 inline-flex w-full items-center justify-center rounded-xl
                   bg-[#5b63ff] hover:bg-[#4f58ff] transition
                   py-3 text-sm font-semibold text-protec-50"
          >
            Solicitar información
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
